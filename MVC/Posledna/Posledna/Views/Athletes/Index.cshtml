@model IEnumerable<Posledna.Models.Athlete>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Level)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AthleteNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Name, "Details", new { id = item.AthleteId })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Level)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AthleteNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AthleteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AthleteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AthleteId })
        </td>
    </tr>
}

</table>
